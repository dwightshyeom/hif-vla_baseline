name: pusht_image_friction_three_tracks

image_shape: &image_shape [3, 96, 96]
shape_meta: &shape_meta
  obs:
    image:
      shape: *image_shape
      type: rgb
    agent_pos:
      shape: [2]
      type: low_dim
  action:
    shape: [2]

env_runner:
  _target_: memory_diffusion_policy.env_runner.pusht_image_friction_three_tracks_runner.PushTImageFrictionThreeTracksRunner
  n_train: 6
  n_train_vis: 6
  train_start_seed: 0
  n_test: 50
  n_test_vis: 50
  legacy_test: False
  test_start_seed: 100000
  max_steps: 1500   # 3 trials × (push + return) at control_hz — give policy room
  n_obs_steps: ${n_obs_steps}
  n_action_steps: ${n_action_steps}
  fps: 10
  past_action: ${past_action_visible}
  # Rollout knobs (overridable from the top-level workspace yaml):
  #   n_envs        — envs in parallel per chunk. null → n_train+n_test.
  #                   Pair a small int with rollout_mode=local on RAM-tight boxes.
  #   rollout_mode  — "wandb" (upload videos to wandb) or "local"
  #                   (save to <output_dir>/epoch_<E>_rollout/; wandb still
  #                   gets the numerical metrics).
  n_envs: ${oc.select:rollout_n_envs,null}
  rollout_mode: ${oc.select:rollout_mode,wandb}
  debug_show_friction: False  # KEEP False for training — reveals which lanes are bad

dataset:
  _target_: memory_diffusion_policy.dataset.pusht_friction_three_tracks_image_dataset.PushTFrictionThreeTracksImageDataset
  zarr_path: data/pusht_friction_three_tracks_demo.zarr
  horizon: ${horizon}
  pad_before: ${eval:'${n_obs_steps}-1'}
  pad_after: ${eval:'${n_action_steps}-1'}
  seed: 42
  val_ratio: 0.02
  max_train_episodes: null
